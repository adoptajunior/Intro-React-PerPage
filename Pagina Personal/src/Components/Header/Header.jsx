import React from 'react'

const Header = (props) => {
    return (
        <h2>Currently {props.cityhome} based, attending class in {props.citytravel}. Just started learning:</h2>
    )
}

export default Header